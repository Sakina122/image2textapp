name: Sync to Hugging Face hub

   on:
   push:
      branches: [main]
      # to run this workflow manually from the Action tab
     workflow_dispatch:

 jobs:
    sync-to-hub:
    runs-on: ubuntu-latest"
steps:
 -uses: actions/checkout@v2
with:
  fetch-depth: 0
- name: Push to hub
  evn:
    $HF-TOKEN: ${{ secrets.$HF-TOKEN}}
    run: git push --force https://sakinafatima123:${HF-TOKEN}@huggingface.co/spaces/sakinafatima123/image-to-text-app main
